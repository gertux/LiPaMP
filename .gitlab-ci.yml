image: maven:latest

build:
  stage: build
  script:
    - mvn compile

test:
  stage: test
  script:
    - mvn org.jacoco:jacoco-maven-plugin:prepare-agent verify

coverage:
  stage: deploy
  script:
    - bash <(curl -s https://codecov.io/bash)
